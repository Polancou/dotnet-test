services:
  sqlserver:
    image: mcr.microsoft.com/azure-sql-edge:latest
    container_name: sqlserver
    cap_add:
      - SYS_PTRACE
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=${DB_PASSWORD}
    ports:
      - "1433:1433"
    volumes:
      - sqlvolume:/var/opt/mssql
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/1433'"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  backend-dotnet:
    profiles: ["dotnet"]
    build:
      context: ./dotnet-backend
      dockerfile: src/Api/Dockerfile
    container_name: backend-dotnet
    environment:
      - ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=CleanArchDb;User Id=sa;Password=${DB_PASSWORD};TrustServerCertificate=True;
      - Jwt__Key=${JWT_KEY}
      - Gemini__ApiKey=${GEMINI_API_KEY}
    ports:
      - "8001:8080"
    depends_on:
      - sqlserver

  backend-python:
    profiles: ["python"]
    build:
      context: ./python-backend
      dockerfile: Dockerfile
    container_name: backend-python
    environment:
      - DATABASE_URL=mssql+pymssql://sa:${DB_PASSWORD}@sqlserver:1433/CleanArchDb
      - DB_PASSWORD=${DB_PASSWORD}
      - JWT_SECRET_KEY=${JWT_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    ports:
      - "8001:8001"
    depends_on:
      sqlserver:
        condition: service_healthy

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    container_name: client
    ports:
      - "8080:80"
    depends_on:
      - sqlserver

volumes:
  sqlvolume:
